export interface RecipeDetail {
    recipeId: number
    recipeName: string
    instructions: string
    image: string
    youtube: string
    category: string
    area: string
    ingredients: string[]
    measures: string[]
}